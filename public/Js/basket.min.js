//déclaration variables
const formulaireElt = document.getElementById("section-formulaire");

console.log("formulaire:" + formulaire);

const totalPriceElt = document.getElementById("total-price");
//--------affichage produit selectionner
//selection de la class ou je vais injecter le code html

const positionElt = document.getElementById("articles");
console.log("positionELt:" + positionElt);
const validation = document.getElementById("validate");
console.log("validation:" + validation);
//Récupération données stockées dans le LS

//Déclaration de la variable "arrayProductsInBasket"dans laquelle on met les key et les values.
arrayProductsInBasket = JSON.parse(localStorage.getItem("products"));
console.log("arrayProductsInBasket:" + arrayProductsInBasket);
//--------affichage produit selectionner
//selection de la class ou je vais injecter le code html

// si le panier est vide
if (arrayProductsInBasket === null) {
  const panierVide = `<main class="container-produits-panier"><div>Le panier est vide !</div></main>`;
  console.log("Je suis vide");
  positionElt.innerHTML = panierVide;
} else {
  console.log("Je ne suis pas vide");
  //si le panier n'est pas vide: afficher les produits dans le LS
  //déclarer une variable tableau vide ici

  let structureProduitPanier = [];

  for (const products of arrayProductsInBasket) {
    structureProduitPanier =
      structureProduitPanier +
      `<section class="container-recapitulatif">
    <div>Quanité-${arrayProductsInBasket.name} vernis : ${arrayProductsInBasket.optionVarnish}</div>
    <div>${arrayProductsInBasket.price} € - supprimer article </div></section>`;
  }
}
